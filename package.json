{
  "name": "aiscast",
  "private": true,
  "workspaces": [
    "signalk-plugin",
    "viewer"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "overrides": {
    "typescript": "$typescript"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
